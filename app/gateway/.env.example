# Copy to app/gateway/.env and adjust. Never commit app/gateway/.env.
#
# Gateway service (GATEWAY_ prefix)
GATEWAY_HOST=0.0.0.0
GATEWAY_PORT=8000
GATEWAY_AUTH_SERVICE_URL=http://auth:8001

# Must match auth AUTH_JWT_SECRET / AUTH_JWT_ALGORITHM
GATEWAY_JWT_SECRET=change-me-use-a-long-random-secret
GATEWAY_JWT_ALGORITHM=HS256
